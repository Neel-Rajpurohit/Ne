import SwiftUI

struct IntroSlide2View: View {
    var body: some View {
        SlideView(
            title: "Personalized for You",
            description: "Enter your school, tuition, and extra class timings once, and we handle the rest.",
            icon: "clock.badge.checkmark",
            color: .green
        )
    }
}
